# 模型现状与下一步工作提示词（2026-08-06 验证后）

> **用途**：本文件汇总《必做清单.md》T0-1~T4-9 全部验证后的模型现状、遗留问题与
> 下一步待办，可直接作为后续会话/Agent 的工作提示词。所有数字可溯源至
> `docs/FINAL_RESULTS.md`（表 2-10）与对应 JSON（见各条目"证据"）。

---

## 一、背景定位

ESP32-S3 便携式心电采集 + TFLite Micro 单拍 AI 异常检测（ResNet-L, INT8 163.5KB, 250 点窗口）。
部署定稿模型 = **exp6-SGD**（部署链重训，SGD lr 0.01, batch 64），已编译级集成固件
（`include/ai_inference/ecg_model_data.h`），`pio run` 通过（RAM 30.0% / Flash 93.9%）。
另新增三个功能模块（编译级集成）：心律安全（停搏/过缓/过速）、AF 检测（RR 不规则度）、
VF/VT 检测（DSP 特征），见 `src/{rhythm_safety,af_detect,vf_detect}/`。

## 二、关键结论（验证已定稿，勿推翻）

1. **主结果口径**：患者级划分 + 未增强测试拍（T1-2）。MIT 域 exp5 0.9295 / exp6 0.8942 /
   P2A 0.9878；PTB 域 exp6 0.8232（域内）。
2. **部署链失配**：PTB 主失配 = 因果滤波（−0.113），其中群延迟窗口错位 ~0.035 可用
   P0 时移补偿（δ*≈−6 样本）恢复；拍级频域逆滤波补偿不可行（250 点 FFT 分辨率限制）。
   **报告数字（0.9122/0.7697）为 δ 对齐语义**。
3. **相位扰动增强训练失败**（T2-5）：±5/±10 两档均无净收益 → 训练侧路线关闭，
   正解 = 时移补偿 + 部署链重训。
4. **INT8 量化本身近无损**（|ΔAUC|≤0.006）；固件二次 softmax 不损 AUC 但压缩概率范围
   [0.27,0.73] → 固件阈值语义漂移。
5. **报警决策层**（⚠️ 2026-08-06 修正，TH §二十九）：事件级确认（K=1, θ=0.3,
   GAP=3）+ 三态为推荐——**但原"FAR 87/千h、Se 1.00"系评估代码缺陷产物已作废**；
   修正后：MIT 事件级 K=1 实为 FAR 54,870/千h / Se 0.621（FAR 较拍级减半但 Se 代价
   显著），**任何策略均未达监护目标 FAR≤42**（心律失常库口径）；PTB 筛查最优 =
   N-of-M 5/10（FAR 11,684/千h / Se 0.973）。真实场景 FAR 待人体实验（H6）校准。
   拍级报警 FAR 10 万/千h 不可用（报警疲劳）结论不变。
6. **新功能验证**：AFDB 窗级 AUC 0.935（30s 窗 CV+Shannon 熵）；VFDB 留出 Se 0.957 /
   MIT-BIH 对照 Sp 0.824（CI 覆盖 0.83）、CUDB 独立 Se 0.936。
7. **"完整波形训练"假设已验证（部分成立，勿重复实验）**：心律失常并非总发生在完整波形上——
   形态类（V 室早）单拍可判（患者级召回 0.952）；关系类（S 室上性早搏）判据在拍间
   （pre-RR 判别 S/N 的 AUC 0.964，Cohen's d=−1.80——"提前"才是异常，形态可正常）；
   节律类（AF/VF/停搏）**无完整波形结构**（DSP 规则已覆盖）。另：当前 250 点窗口 99%
   混入相邻拍残段（模型已隐式利用碎片上下文）→ 以"完整波形"为训练对象预计无增益。
   证据 `models/waveform_hypothesis_eval.json`。

## 三、模型遗留问题（按优先级）

### P0 — 固件一致性（让"板上数字 = 论文数字"）
1. **固件删除二次 softmax**（`src/ai_inference/ai_inference.cpp` 反量化后直接取概率，
   不再 softmax；证据 `models/int8_nosoftmax_eval.json`）。改动后 `pio run` 编译检查，
   并同步 README 推理步骤描述。验收：编译通过 + 概率范围恢复 [0,1]。
2. **固件加窗口偏移补偿（P0）**：AI 输入窗口相对 R 峰提前 6 样本（24ms）以抵消因果链
   群延迟（T1-3 δ*）。可在 `ai_inference_push` 或 R 峰检测后实现，零计算成本。
   验收：`pio run` 通过；PC 侧用 `eval_deploy_compensation.py` 的 δ 曲线核对。
   ⚠️ 此两项不做，板上实际性能将比 FINAL_RESULTS 表 4 低 0.02~0.04。

### P1 — 真实场景鲁棒性（需硬件/人体实验，当前挂起）
3. **真实佩戴验证**：模型对 R 峰偏移敏感（PTB 域 δ=−10 掉 0.11），心率检测器 LUDB
   Se 仅 72.9% → 真实 QRS 错位会打向 AI 性能。待伦理审批 + 人体实验（H6）后，
   用 `docs/hardware/ondevice_bench_protocol.md` 实测；SQI 门控/三态阈值当前为
   概率边际代理，真实 SQI 分布未知。
4. **板上实测**：INT8 与 PC 一致性（`ondevice_bench_protocol.md` §3）、VF/AF 模块的
   固件特征近似（med_abs/主频近似）与 PC 精确版一致性。

### P2 — 已知性能边界（如实报告，不建议硬啃）
5. **PTB 部署链残余缺口 ~0.05**：因果滤波固有失真，时移补偿无法消除；降 HP 0.05Hz
   已实施。仅剩"线性相位滤波"路线（成本高，需固件滤波改造，评估后再定）。
6. **SVEB/F 类单拍瓶颈**：单拍信息不足，非模型缺陷；上下文需求已用 RR 特征/DSP
   规则覆盖（见 §四.3）。
7. **双专家 OR 部署受 Flash 阻塞**：单模型 163.5KB → Flash 93.9%；双模型需先解决
   预算（模型压缩/分区调整/分时加载），PC 侧评估不受影响。

## 四、明确不需要做的事（避免返工）

1. **不要训练记录级划分模型**：记录级划分泄漏（66% 测试拍来自训练患者，
   `models/leakage_audit.json`）；患者级划分已定稿。
2. **不要重跑相位扰动增强训练**（T2-5 已证负面结果）。
3. **不要训练 3-beat/长窗模型**：D5 已否决（数据量不足，recall 崩至 67%）。
4. **不要为板上功能训练"记录级/节律级"模型替代 DSP 规则**：AF（AUC 0.94）、VF/VT
   （Se 0.957）已用零训练 DSP 方案达标（架构计划"零训练优先"原则）——**板上路线不变**。
   ⚠️ 此条**不适用于云端/新功能**：云端模式 A（多类诊断）与"30s 长程节律分类"等
   新功能应使用 PTB-XL（见 §四·五），属独立立项，不算"修补当前模型"。
5. **不要训练"完整波形对齐"模型**：已验证（`verify_waveform_hypothesis.py` →
   `models/waveform_hypothesis_eval.json`）——瓶颈在**上下文缺失**而非波形边界截断：
   S 类需 RR 上下文（pre-RR AUC 0.964，已由 RR 特征/规则覆盖）、节律类无波形（DSP 规则
   已覆盖）、V 类单拍已 0.952；更宽窗口受 ESP32 内存约束（D5 已否决 3-beat）。

## 四·五、数据库资产要点（含 PTB-XL / LUDB 定位）

> 两套标签体系互补：**记录级/节律级库**（PTB/VFDB/CUDB/AFDB/PTB-XL）回答"这段是什么
> 状态"；**拍级/波形级库**（MIT-BIH/INCART/LUDB）回答"这个拍是什么"。

| 库 | 标签粒度 | 定位 | 已用 | 未用价值 |
|----|---------|------|------|---------|
| PTB | 记录级（MI/对照） | MI 训练+域内评估 | ✅ | — |
| VFDB / CUDB | 节律级 / 记录级 | VF 检测训练+独立测试 | ✅ | — |
| AFDB | 节律级（AF 时段） | AF 检测验证（AUC 0.935） | ✅ | — |
| **PTB-XL** | 记录级（诊断 5/44 类 + 节律 12 类） | ⚠️ 三次失败记录（直接合并崩溃/两阶段 Recall↓/SSL 无增益，TH §三/§六）——**教训：记录级标签不可下放训拍级模型** | 节律标签、云端多类诊断、ST 形态学预研（见 §五.5-7） |
| MIT-BIH / INCART | 拍级（AAMI） | 心律失常训练+主结果 | ✅ | — |
| **LUDB** | 拍级+**波形级**（QRS 峰 + P-QRS-T 边界） | 心率验证（F1 0.774） | **波形边界未利用** → 模块 4 ST 段测量验证金标准；真实 R 峰错位误差分布量化 |

**PTB-XL 正确打开方式**（最大规模库，21,837 条 × 12 导联 × 10 秒）：只做**记录级任务**，
不做拍级下放。正面价值 = 云端多类诊断（模式 A 数据源）、节律标签（AF 短窗验证）、
ST 形态学预研（模块 4 地基）、罕见类别覆盖。

## 五、下一步待办（按顺序）

| # | 任务 | 证据/工具 | 验收 |
|---|------|----------|------|
| 1 | 固件删二次 softmax + 加 δ 补偿（P0-1/2） | `int8_nosoftmax_eval.json`、`deploy_compensation_eval.json` | `pio run` 通过；README 同步 |
| 2 | 固件阈值重扫描（若保留 softmax 语义则按固件概率重标 θ） | `eval_alarm_decision.py` 参数化 | θ 表更新至 FINAL_RESULTS 表 6 |
| 3 | 人体实验/板上实测（待伦理批准） | `docs/hardware/*.md` | 按协议验收标准 |
| 4 | 双专家 Flash 预算方案评估（压缩/分区） | `best_resnet_large_exp6_phase*.h5` 尺寸 | 可行性报告 |
| 5 | **AF 短窗验证（PTB-XL 节律标签）**：现有 CV+熵规则跑 PTB-XL 10s 片段（`ptbxl_rhythm_processed.npz`），报告 10s 窗 AFIB 判别 AUC——支撑消费设备"一键测房颤"（若达标则入 FINAL_RESULTS 表 6 补充行） | `eval_rhythm_af.py` 参数化（窗长 10s） | AUC 报告 + 与 30s 窗对比结论 |
| 6 | **ST 形态学预研（模块 4 地基）**：PTB-XL 子类标签（前壁/下壁/STTC）+ J+80ms 测量统计 ST 偏移与标签关系；LUDB 波形边界验证 J 点定位精度 | `data/preprocess_ptbxl.py`、LUDB 边界标注 | 可行性统计报告（不入主结果） |
| 7 | **云端多类诊断预研（模式 A 立项时）**：PTB-XL 记录级 5/44 类分类器原型；可选同域 SSL（PTB-XL→PTB）对照 | 产品决策先行 | 立项后评估 |

## 六、执行约束（AGENTS.md）

- 固件改动仅 `pio run` 编译检查，不烧录、不要求用户操作硬件。
- 改动前后 `git status` 检查；只提交用户明确要求的文件；大文件（.h5/.tflite/数据库）
  不提交（数据存 WSL 本地 `/home/devcontainers/`）。
- 所有数字变更同步 `docs/FINAL_RESULTS.md`，保持 JSON → 台账 → 文档可溯源。
- 重大决策（换模型/改评估口径/固件滤波改造）前置：联网文献核实 + git status +
  `TUNING_HISTORY.md` 追加章节留痕。
- 决策自主执行；仅在无法自动恢复的异常时暂停向用户汇报。
