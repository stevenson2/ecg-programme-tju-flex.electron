# ESP32-S3-SUPERMINI 功耗诊断指南

## 现有固件状态

当前固件已正常工作（温度监测 + CPU 80MHz + BLE +3dBm + 串口 25Hz），温度稳定在 **~55-65°C**。

## 电流表检测步骤

你需要：**一个万用表（电流档）**

### 检测点 1：USB 入口总电流

将万用表串入 USB 5V 供电回路：

```
USB 5V ─── 万用表(电流档) ─── SUPERMINI 的 USB 5V 输入
```

1. 拔掉 USB 线
2. 万用表拨到 **200mA 或 10A 档**
3. 红表笔接 USB 5V，黑表笔接 SUPERMINI 的 5V 输入脚
4. 插上 USB，记录当前固件的总电流读数

### 检测点 2：3.3V 稳压输出电流

用镊子找到 SUPERMINI 上 **3.3V 稳压器**（通常是一个小 3 脚或 5 脚 IC），测其输出脚电流：

```
3.3V 稳压器输出 ─── 万用表 ─── 板子其余电路
```

### 检测点 3：各电阻对地电流

用镊子测量最热电阻两端的 **压降**，计算功耗：

| 电阻标识 | 测量方法 | 正常值 |
|---------|---------|--------|
| LED 限流电阻 (靠近 GPIO48) | 测两端电压 ÷ 电阻值 | < 5mA |
| 上拉电阻 (靠近 GPIO0) | 测两端电压 ÷ 电阻值 | < 0.1mA |
| USB D+/D- 串联电阻 | 测两端电压 ÷ 电阻值 | < 0.1mA |

## 预期电流参考值

| 运行状态 | 3.3V 侧电流 | USB 5V 侧电流 | 温度 |
|---------|------------|--------------|------|
| 空闲 40MHz (无 BLE) | ~30mA | ~50mA | <35°C |
| CPU 80MHz + BLE adv | ~50mA | ~80mA | ~40°C |
| CPU 240MHz + BLE +9dBm | ~120mA | ~200mA | ~60°C |
| **当前固件 (80MHz, +3dBm)** | **~60mA** | **~100mA** | **~55-65°C** |

## 判断方法

| 测量结果 | 结论 |
|---------|------|
| USB 侧电流 > 150mA | **LDO 效率低是主要热源** → 改用 3.3V 直供 |
| 3.3V 侧 > 80mA | **ESP32 芯片功耗偏高** → 检查是否有 GPIO 短路 |
| 测量结果正常但板子仍烫 | **PCB 尺寸太小、散热不足** → 加散热片 |
| 某电阻两端电压异常 | **该电阻回路可能短路** |

## 短路排查

如果怀疑短路，断电后打到 **电阻档**（或二极管档）测：

| 测点 | 对地电阻正常值 | 短路值 |
|------|--------------|--------|
| 3.3V 对 GND | >1kΩ | <10Ω |
| GPIO48 (LED) 对 GND | >10kΩ (LED灭时) | <10Ω |
| GPIO0 对 GND | >10kΩ (不按下时) | <10Ω |
| USB D+ 对 GND | >1kΩ | <10Ω |