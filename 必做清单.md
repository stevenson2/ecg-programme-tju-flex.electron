# 必做清单（科研 × 产品共同需要，按重要程度排序）

> **日期**：2026-08-05
> **来源**：综合 `solutions.md`（审稿问题）、创新方向文献判定、消费电子场景价值分析、`consumer_ecg_architecture_plan.md`
> **原则**：项目是主线，论文是附属；每项任务优先让设备/系统受益，论文材料是副产品。
> **排序依据**：科研价值 × 产品价值的交集大小 × 紧迫性（是否阻塞其他工作）。
> **执行约束**（AGENTS.md）：不涉及硬件烧录（固件改动仅 `pio run` 编译检查）；改动前/后 `git status` 检查；重大决策需联网文献核实 + `TUNING_HISTORY.md` 留痕；大文件（.h5/.tflite）不提交。

---

## 清单总览

| 编号 | 任务 | 科研价值 | 产品价值 | 验收标准 | 工作量 |
|------|------|---------|---------|---------|:---:|
| **T0-1** | exp6-SGD 固件集成（编译级） | 解决 H1/M4/L2/M12 部署叙事断裂 | 设备从"旧 CNN-v2"升级到部署链定稿模型 | `pio run` 通过；头文件模型身份确认；README 与实物一致 | 0.5 天 |
| **T1-2** | 未增强测试拍重评（M5） | 审稿最实质问题；主结果可信 | 真实（非增强）性能数字 | 未增强测试集主结果更新，FINAL_RESULTS.md 可溯源 | 1 天 |
| **T1-3** | 部署链失配分解 + 输入侧补偿 | 核心贡献深化（分解/预测/补偿三件套）；M1/M13 证据 | 旧模型不重训即少掉部分 ΔAUC −0.105；滤波改造决策依据（D7） | 分量消融表 + 补偿 vs 重训对比表 | 2 天 |
| **T1-4** | 报警决策层：误报率指标 + 融合粒度对比 + 三态输出 | 补 H2/M9 融合数据；架构计划证据 | 报警层参数（确认窗数/粒度）由数据决定，防误报疲劳 | 粒度-误报-时延权衡表 + 推荐确认策略 | 1-2 天 |
| **T2-5** | 全类相位抖动增强训练 + 相位鲁棒性指标 | 真实空白方向（首个量化 + 指标） | 对真实 R 峰检测误差（LUDB Se 72.9%）鲁棒 | 测试时偏移敏感性曲线 + 增强前后部署链 AUC 对比 | 1 天 |
| **T3-6** | 评估可信度补全：bootstrap CI（M8）+ 泄漏审计脚本（M10）+ INT8 去 softmax 对照（M3）+ S 类构成分析（M6） | 审稿四项中severity问题 | 数字可信度（间接） | 4 项产物 + 论文 [待补充] 标记替换 | 1-2 天 |
| **T3-7** | 写作修订包（P0 全部）：M1 措辞、M2 阈值/缩放说明、M7 架构表、M11 拍数口径、M12 声明、M13 范围、L1 术语、H3 配置表 | 投稿前必须 | 文档与实物一致 | 论文/报告/README 修订完成，数字与 FINAL_RESULTS 一致 | 1-2 天 |
| **T4-8** | 模块 1+3：心律安全逻辑（停搏/过缓/过速）+ AF RR 不规则度检测 | 架构计划验证；AF 分支（R1/R4 范式） | 消费设备核心价值（危急报警 + AF 筛查通知） | 模拟器回放测试通过；AFDB 验证 AUC 报告 | 1-2 周 |
| **T4-9** | 模块 2：VF/VT 检测器（ZCR/PPI） | VF 分支（R5/R6 验证） | 秒级危急报警 | VFDB 独立测试 Se≥95%/Sp≥83% 报告 | 1-2 周 |

**前置阻塞（不阻塞 T0-T4 执行，但决定最终产品形态）**：真实佩戴数据校准——需伦理审批 + 人体实验，获批前所有"个人基线/真实场景验证"挂起。

---

## 分阶段执行顺序

### T0 地基（先做，阻塞所有"部署可信"叙事）
- **T0-1 exp6-SGD 固件集成**：从 `best_resnet_large_exp6_sgd.h5`（对应 `deploy_match/retrain_exp6_sgd_eval.json`）经 `export.py` 导出 INT8 TFLite → 生成 C 头文件 → 替换 `include/ai_inference/ecg_model_data.h` → `pio run` 编译检查通过。
  - 同步：README 核心配置表（模型身份/大小）、论文 §3.4/§5.2 部署叙事改写（H1/M4/L2/M12 一并解决）。
  - 注意：若后续 T1-4/T2-5 产出更优模型，T0-1 可重做；但"先有真模型上板"是硬前提。

### T1 科研 × 产品最大交集（做完设备与论文同时上一个台阶）
- **T1-2 未增强测试重评**：用 `--no-augment` 预处理重建未增强测试拍（训练仍用 6× 增强数据不变）→ 同一患者级划分重评 exp4/5/6/P2A → 更新 `patient_split_eval.json`、`FINAL_RESULTS.md`、论文表 T8。
- **T1-3 部署链失配分解 + 输入侧补偿**：复用 `eval_deploy_match.py` 消融链，分解各分量（因果 biquad 相位失真 / 瞬态响应 / HP 截止漂移 / 2:1 抽取 / INT8 量化）对 ΔAUC 的贡献；实现输入侧补偿原型（逆滤波/相位校正，参考 `papers/` 中 Pickett2023、Hnatiuk2025），评估"补偿 + 旧模型（不重训）" vs "部署链重训" 的性能对比。
- **T1-4 报警决策层**：
  - 定义误报率指标：**每千小时可行动报警数** = f(拍误报率、确认窗数、SQI 门控、场景阈值)；
  - 融合粒度对比：同一模型上系统对比 拍级 OR / 分数加权 / 段级确认 / 事件级确认（复用 `eval_fusion.py`、`eval_expert_combo_patient.py`、`sim_temporal_agg.py`、TUNING_HISTORY §8.9.9 段级逻辑）；
  - 三态输出：正常 / 异常 / **无法判定**（R4 equivocal 范式，SQI 驱动）；
  - 产出：粒度-误报-时延权衡表 → 直接给架构计划模块 2/3/4 的确认策略定参数（论文 H2/M9 数据同步补齐）。

### T2 部署鲁棒性（新研究方向，科研空白 + 产品刚需）
- **T2-5 全类相位抖动增强训练 + 相位鲁棒性指标**：
  - 训练侧：正常/异常两类**一起**做相位扰动（±2/±5/±10 样本，避免 D7 单类移位捷径教训）→ 部署链口径重训；
  - 评估侧：测试时 R 峰偏移（±0/2/5/10/20 样本）→ 画"偏移量 vs AUC"敏感性曲线，定义拍级相位鲁棒性指标（与通用时序 S-Cons. 区分）；
  - 产出：增强前后部署链 AUC 对比 + 敏感性曲线，入论文作为新评估协议。

### T3 评估可信度 + 写作（投稿前必须，产品受益间接）
- **T3-6**：① bootstrap CI：患者级重采样（复用 `eval_deploy_match.py` 的 `_patient_bootstrap_delta_auc` 模式）跑主结果 13 模型；② 泄漏审计脚本：统计记录级划分下 PTB 测试拍来自训练患者的比例（复用 `verify_split_consistency.py` 交集逻辑），论文 §4.2 补方法句；③ INT8 对照：`eval_deploy_match.py` 加 `--no-double-softmax` 分支重跑，分离量化误差与 softmax 压缩；④ S 类构成分析：按记录/患者分解 SVEB 召回验证"构成效应"。
- **T3-7**：M1 措辞（"approximately half (≈49%)"+ 统一 39% 口径）、M2 阈值描述（自适应比例 0.30 + LUDB ×1000 缩放，三文档同步）、M7 架构表（源自 `resnet_lite_1d.py`）、M11 拍数口径（834,741 拍 / 834,495 三拍序列）、M12 部署声明、M13 范围收敛、L1 术语统一、H3 训练配置表。

### T4 产品价值主线（架构计划模块落地，新功能）
- **T4-8 模块 1+3**：心律安全逻辑（停搏 ≥4s / 重度过缓 <40bpm≥30s / 过速 >180bpm≥30s，纯逻辑，SQI 门控）+ AF RR 不规则度（变异系数/Shannon 熵，30-60s 窗，三档输出）；回放测试（模拟器模式）+ AFDB 验证。
- **T4-9 模块 2**：ZCR/PPI VF/VT 检测器（3-5s 窗 + 连续 2 窗确认）；VFDB/CUDB 训练与独立测试。

---

## 附：AI 执行提示词

```
你是一个严格执行者。任务：按项目根目录《必做清单.md》执行全部任务，从 T0-1 开始按编号顺序推进，
前一项验收通过才进入下一项。遵守项目 AGENTS.md 全部规范。

【总原则】
1. 项目是主线，论文是附属：每项任务优先让设备/系统受益，论文材料是副产品。
2. 当前阶段不涉及硬件部署：固件改动只做编译检查（pio run），不烧录、不要求用户操作硬件。
3. 每次改动前运行 git status 检查工作区；每次成功生效后再 git status 确认变更范围。
   只提交用户明确要求的文件；大文件（.h5/.tflite/模型权重）不得擅自提交。
4. 重大决策（重训/换模型/改评估口径/取舍方案）前置检查：联网检索+文献核实支撑结论、
   git status 确认工作区状态，并在 TUNING_HISTORY.md 末尾追加"第 N 章"留痕
   （决策背景、证据、执行方案、结果与影响）。
5. 决策自主执行，不暂停询问；仅在无法自动恢复的异常（训练崩溃且无法定位、环境故障、
   文件时序锁被破坏）时才暂停并向用户汇报。
6. 所有数字变更必须同步 docs/FINAL_RESULTS.md 并保持可溯源（JSON → 台账 → 文档）。

【每项任务的关键约束与验收】（详见《必做清单.md》对应编号）
- T0-1：exp6-SGD 权重 best_resnet_large_exp6_sgd.h5 → export.py 导出 INT8 → 替换
  include/ai_inference/ecg_model_data.h → pio run 通过；同步 README 与论文部署叙事（H1/M4/L2/M12）。
- T1-2：--no-augment 重建未增强测试集（训练数据不变），患者级划分重评，更新主结果与溯源链。
- T1-3：部署链失配分量消融表 + 输入侧补偿原型（逆滤波/相位校正），补偿 vs 重训对比。
- T1-4：误报率指标定义 + 融合粒度对比表 + 三态输出（含"无法判定"档），给出确认策略推荐参数。
- T2-5：两类一起相位扰动增强训练（严禁单类移位）+ 测试时偏移敏感性曲线 + 相位鲁棒性指标。
- T3-6：bootstrap CI、泄漏审计脚本、INT8 去 softmax 对照、S 类构成分析，四项产物齐全。
- T3-7：写作修订包八项全部完成，论文/报告/README 数字与 FINAL_RESULTS.md 一致。
- T4-8/T4-9：模块 1+3（心律安全+AF）、模块 2（VF/VT）实现 + 回放测试/AFDB/VFDB 验证报告。

【完成后】输出汇总：每项任务的结果、证据文件路径、遗留问题、git status 变更范围。
```

---

*本清单为项目执行的事实来源；执行中的决策留痕与文档更新按 AGENTS.md 第 7 节执行。*
